; Electron requires its postinstall (binary fetch) — explicit, reviewed opt-out
; from the root ignore-scripts hardening. Do not add other script-running deps
; here without review.
ignore-scripts=false
