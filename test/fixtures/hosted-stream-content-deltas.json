{
  "description": "Synthetic merge_intervals answer captured from hosted SSE; content deltas only, no tenant data or serving metadata.",
  "deltas": [
    "",
    "def",
    " merge_intervals(intervals",
    "):\n    if",
    " not intervals:\n",
    "        return []\n",
    "    sorted_intervals =",
    " sorted([",
    "list(i",
    ") for i in",
    " intervals],",
    " key=lambda x:",
    " x[0])",
    "\n    merged =",
    " [sorted_intervals[",
    "0][:",
    "]]\n    for",
    " current",
    " in sorted_intervals[",
    "1:]:\n",
    "        if current[",
    "0] <= merged",
    "[-1][1",
    "]:\n            merged",
    "[-1][1",
    "] = max(",
    "merged[-1][",
    "1], current[",
    "1])\n       ",
    " else:\n           ",
    " merged.append(current[:",
    "])\n    return",
    " merged"
  ],
  "expected": "def merge_intervals(intervals):\n    if not intervals:\n        return []\n    sorted_intervals = sorted([list(i) for i in intervals], key=lambda x: x[0])\n    merged = [sorted_intervals[0][:]]\n    for current in sorted_intervals[1:]:\n        if current[0] <= merged[-1][1]:\n            merged[-1][1] = max(merged[-1][1], current[1])\n        else:\n            merged.append(current[:])\n    return merged"
}
